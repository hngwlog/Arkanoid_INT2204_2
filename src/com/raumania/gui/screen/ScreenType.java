package com.raumania.gui.screen;

/** Enum of all screens in the game. */
public enum ScreenType {
    HOME,
    SETTINGS,
    LEVEL_SELECT,
    GAME,
    MULTIPLAYER,
    GAME_OVER,
    SKIN_SELECT,
}
